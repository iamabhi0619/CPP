#include<bits/stdc++.h>
using namespace std;
int main()
{
    //collection of variable of same datatype
    int arr[5];
    cout<<"Enter the value in array:- ";
    for (int i = 0; i < 5; i++)
    {
        cin >> arr[i];
    }
    cout << "Value in array:- ";
    for (int i = 0; i < 5; i++)
    {
        cout << arr[i] << " ";
    }
    return 0;
}
